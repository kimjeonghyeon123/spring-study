<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.earth.korea.dao.UserChatRoomMapper">

	<!-- 채팅방 번호 가져오기 -->
	<select id="selectChatRoomId" parameterType="map" resultType="int">
		SELECT chatroom_id
		FROM t_userchatroom
		WHERE login_id = #{login_id} and other_id = #{other_id}
	</select>

	<!-- 채팅방 생성하기 -->
	<insert id="insert" parameterType="UserChatRoomDto">
		INSERT INTO t_userchatroom(chatroom_id, login_id, other_id) 
		VALUES(#{chatroom_id}, #{login_id}, #{other_id}),
		       (#{chatroom_id}, #{other_id}, #{login_id})
	</insert>

</mapper>