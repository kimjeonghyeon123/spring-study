<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"https://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.earth.jeonghyeonkim.dao.DangBoardMapper">
	
	<sql id="selectFromBoard">
		SELECT bno, title, writer, reg_date, view_cnt
		FROM dang_board
	</sql>
	
	<sql id="searchCondition">
				
	</sql>
	
	<select id="count" resultType="int">
		SELECT COUNT(*)
		FROM dang_board;
	</select>
	
	<select id="select" parameterType="int" resultType="DangBoardDto">
		<include refid="selectFromBoard" />
		WHERE bno = #{bno}
	</select>

	<select id="selectAll">
		<include refid="selectFromBoard" />
		ORDER BY reg_date, bno desc
	</select>
	
	<select id="selectPage" parameterType="map" resultType="DangBoardDto">
		<include refid="selectFromBoard" />
		ORDER BY reg_date, bno desc
		LIMIT #{pageSize} OFFSET #{offset}
	</select>

	<select id="searchResultCnt" parameterType="SearchItem" resultType="int">
		SELECT COUNT(*)
		FROM dang_board
		WHERE true
		<include refid="searchCondition" />
	</select>
	
	<select id="searchSelectPage" parameterType="SearchItem" resultType="DangBoardDto">
		<include refid="selectFromBoard" />
		WHERE true
		<include refid="searchCondition" />
		ORDER BY reg_date desc, bno desc
		LIMIT #{pageSize} OFFSET #{offset}
	</select>
	
	<delete id="deleteAll">
		DELETE FROM dang_board
	</delete>
	
	<delete id="delete" parameterType="map">
		DELETE FROM dang_board
		WHERE bno = #{bno} AND writer = #{writer}
	</delete>
	
	<delete id="deleteForAdmin" parameterType="int">
		DELETE FROM dang_board
		WHERE bno = #{bno}
	</delete>
	
	<insert id="insert" parameterType="DangBoardDto">
		INSERT INTO dang_board(title, content, writer)
		VALUES(#{title}, #{content}, #{writer})
	</insert>
	
	<update id="update" parameterType="DangBoardDto">
		UPDATE dang_board
		SET title = #{title}, content = #{content}, up_date = now()
		WHERE bno = #{bno} AND writer = #{writer}
	</update>
	
	<update id="increaseViewCnt" parameterType="int">
		UPDATE dang_board
		SET view_cnt = view_cnt + 1
		WHERE bno = #{bno}
	</update>
</mapper>






















