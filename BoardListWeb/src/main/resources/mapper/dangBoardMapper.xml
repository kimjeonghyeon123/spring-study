<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"https://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.earth.jeonghyeonkim.dao.DangBoardMapper">
	<sql id="selectFromBoard">
		SELECT bno, title, writer, reg_date, view_cnt
		FROM dang_board
	</sql>

	<select id="select" parameterType="int" resultType="DangBoardDto">
		<include refid="selectFromBoard" />
		WHERE bno = #{bno}
	</select>
	
	<select id="selectAll" resultType="DangBoardDto">
		<include refid="selectFromBoard" />
		ORDER BY reg_date desc, bno desc
	</select>
	
	<select id="selectPage" parameterType="map" resultType="DangBoardDto">
		<include refid="selectFromBoard" />
		ORDER BY reg_date desc, bno desc
		LIMIT #{pageSize} OFFSET #{offset}
	</select>
	
	<select id="searchResultCnt" parameterType="SearchItem" resultType="int">
		SELECT COUNT(*)
		FROM dang_board
		WHERE true
		<include refid="searchCondition" />
	</select>

	<select id="searchSelectPage" parameterType="SearchItem" resultType="DangBoardDto">
		<include refid="selectFromBoard" />
		WHERE true
		<include refid="searchCondition" />
		ORDER BY reg_date desc, bno desc
		LIMIT #{pageSize} OFFSET #{offset}
	</select>
		
	<select id="count" resultType="int">
		SELECT COUNT(*)
		FROM dang_board
	</select>
	
	<sql id="searchCondition">
				
	</sql>
	
	<update id="increaseViewCnt" parameterType="int">
		UPDATE dang_board
		SET view_cnt = view_cnt + 1
		WHERE bno = #{bno}
	</update>
	
	<insert id="insert" parameterType="DangBoardDto">
		INSERT INTO dang_board(title, writer, content) 
		VALUES(#{title}, #{writer}, #{content})
	</insert>
</mapper>