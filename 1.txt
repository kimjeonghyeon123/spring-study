데이터베이스 테이블 하나 당 만들어야 될 것
- DTO 클래스
- Mapper.xml
	- 해당 테이블 내에서 변화가 일어나는 것을 만들어놓아야 함


ex)
게시물에 댓글을 달았을 때
	- 댓글 테이블에 댓글을 insert 한다.
	- 보드의 댓글 수를 1 올린다.
	- CommentService에서 
		- CommentDAO에서 댓글 데이터 넣음
		- BoardDAO에서 댓글 수 1 올림


댕근 마켓 클릭 시 불러와야 될 것
아래 변수들은 service 단에서 처리해주면 됨
상품dto - 로그인한 유저가 찜 했는지 여부 변수 추가
상품dto - 로그인한 유저의 이름 변수 추가
- 상품 타입
	- danggeun_type 테이블
- 판매 지역
	- local 테이블
- 상품 리스트
	- danggeun 테이블
	- local 이름
	- 찜 했는지 여부
- 상품 찜 했는지
	- useremail로 검색해서 상품 id 받아옴
	- id

drop table if exists danggeun;
create table danggeun(
	id 			serial 		primary key,-- 상품아이디
	title		varchar(50) not null,	-- 상품게시물제목
	name 		varchar(50) not null,	-- 상품이름
	type_id		Integer		not null,	-- 상품타입
	local_id	Integer		not null,	-- 판매지역
	price		int			not null,	-- 상품가격
	content 	text 		not null,	-- 상품설명
	writer 		varchar(50) not null,	-- 판매자
	view_cnt 	int 		default 0,	-- 조회수
	add_cnt		int			default 0,	-- 찜 수
	reg_date 	date 		default current_timestamp,
	up_date 	date 		default current_timestamp,
	foreign key (type_id) references danggeun_type(id)
);

create table danggeun_type(
	id serial primary key,
	name varchar(50) not null
);

drop table if exists store_danggeun;
create table store_danggeun(
	member_email varchar(50),
	danggeun_id Integer,
	primary key(member_email, danggeun_id),
	foreign key (member_email) references dang_member(email),
	foreign key (danggeun_id) references danggeun(id)
);


