<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.earth.withdang.dao.ChatUserChatRoomMapper">

	<!-- 상대가 채팅방 삭제했는지 확인 -->
	<select id="selectChatRoomCnt" parameterType="int" resultType="int">
		SELECT COUNT(*)
		FROM tb_chat_userchatroom
		WHERE chatroom_id = #{chatroom_id}
	</select>
	
	<!-- 채팅방 번호 가져오기 -->
	<select id="selectChatRoomId" parameterType="map" resultType="int">
		SELECT chatroom_id
		FROM tb_chat_userchatroom
		WHERE login_nickname = #{login_nickname} and other_nickname = #{other_nickname}
	</select>

	<!-- 채팅방 생성하기 -->
	<insert id="insert" parameterType="ChatUserChatRoomDto">
		INSERT INTO tb_chat_userchatroom(chatroom_id, login_nickname, other_nickname) 
		VALUES(#{chatroom_id}, #{login_nickname}, #{other_nickname}),
		       (#{chatroom_id}, #{other_nickname}, #{login_nickname})
	</insert>

	<!-- 채팅방 삭제하기 -->
	<delete id="delete" parameterType="map">
		DELETE FROM tb_chat_userchatroom
		WHERE chatroom_id = #{chatroom_id} and login_nickname = #{login_nickname}
	</delete>
	
</mapper>